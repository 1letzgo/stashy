query CleanupPerformersBasic($page: Int!, $perPage: Int!) {
  findPerformers(filter: { page: $page, per_page: $perPage, sort: "name", direction: ASC }) {
    count
    performers { id name scene_count image_count gallery_count }
  }
}
